The platform will automatically install artifacts found in this directory:
    - OSGi bundles/fragments/subsystems: just drop the file ;
    - Stamina addons: addons should be installed using the addon:install command,
      but you can only also drop the file here.
